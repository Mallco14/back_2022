let app,dotenv;_292‍.w("./app",[["app",["app"],function(v){app=v}]]);_292‍.w("dotenv",[["*",null,function(v){dotenv=v}]]);

dotenv.config()
const {
    PORT,
} = process.env

const port = PORT;

app.listen(port, () => console.log(`Server init in localhost:${port}`));
