let TestRouter;_b2f‍.x([["routes",()=>routes]]);_b2f‍.w("../components",[["TestRouter",["TestRouter"],function(v){TestRouter=v}]]);

const listRoutes = [["/test", TestRouter]];

       const routes = (app) => {
  listRoutes.forEach(([path, controller]) => {
    app.use(path, controller);
  });
};
